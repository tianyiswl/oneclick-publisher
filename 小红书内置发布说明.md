# 一键发小红书内置发布说明

## 运行边界

一键发的小红书运行时位于 `app_core/xhs_native_adapter.py` 与
`app_core/xhs_publish_executor.py`，覆盖图文、视频、立即发布、定时发布、
话题、AI/原创声明和平台结果回读。

运行时明确不依赖：

- 蚁小二客户端；
- `yxer` 命令行；
- 蚁小二任务网关；
- 蚁小二账号目录；
- 蚁小二远程签名服务；
- 旧发射台/旧小红书 uploader 的最终执行入口。

旧模块名 `xhs_yixiaoer_adapter.py` 仅保留兼容导入，内部会转到一键发内置
适配器，不包含外部调用。

## 从恢复代码迁入的规则

- 小红书只开放图文和视频，纯文字在任务创建前阻断；
- 图文支持 1 至 18 张图片，第一张图片作为封面；
- 视频每个任务只接收一个视频，可选独立图片封面；
- 标题最多 20 字，正文最多 1000 字，话题最多 10 个；
- 可见性沿用恢复任务模型的公开、私密、好友编码；
- 定时值按 `Asia/Shanghai` 转换，并在平台页面回读；
- 只有平台成功页或明确成功提示才可记录为已发布或已定时。

## 签名说明

恢复出的蚁小二直接 HTTP 发布器会向蚁小二自己的签名服务请求小红书动态请求
参数。该签名不是普通开发者向小红书申请的 API Key，也不是一键发可以合法复用
的固定密钥。它属于网页请求的动态校验机制。

一键发不复制、伪造或绕过这套校验，也不调用蚁小二签名服务。当前内置适配器
只在已登录的小红书官方创作页面内操作，由官方页面自身生成必要请求参数。未来
若小红书开放适用于该发布场景的官方接口，应优先改为官方 OAuth/开放平台授权。

## 验证层级

离线测试只能证明字段映射、内容类型、素材限制、封面顺序、定时转换和外部依赖
边界正确。选择器、上传结果、声明、定时和最终回执仍须在用户明确授权后，用一个
测试账号分别完成图文和视频的真实预检；预检不得保存草稿或发布。

2026-08-02 已在用户授权下完成两条真实 dry-run：

- 图文：2 张测试图、标题、正文、1 个官方话题与定时字段回读一致；
- 视频：1 个测试视频、自定义封面、标题、正文、1 个官方话题与定时字段回读一致；
- 两次均未保存草稿、未预览、未发布，测试浏览器已关闭。
