# 一键发内容包 v1

发布中心按内容形态提供三个入口：导入视频包、图文包、文字包。一个内容包可以复用到多个平台，平台差异只写在可选覆盖字段中。

导入只会把本地内容和素材带入编辑器，并强制保持“预发布检查”。不会登录、上传、保存草稿或发表。

## 通用结构

```text
我的内容包/
├── manifest.json
├── 正文.md
├── 封面_竖.png
├── 封面_横.png
└── 素材/（视频包或图文包使用）
```

`manifest.json`、正文文件都必须使用 UTF-8 编码。所有路径只能指向内容包内的相对文件，不能放账号 Cookie、登录信息或自动发表配置。

## 图文包示例

```json
{
  "schemaVersion": "oneclick-content/v1",
  "contentType": "article",
  "title": "通用标题",
  "bodyFile": "正文.md",
  "assets": [
    {
      "path": "素材/01.png",
      "placement": "after_intro"
    },
    {
      "path": "素材/02.png",
      "placement": "after_heading",
      "anchor": "这一节的标题"
    }
  ],
  "covers": {
    "3:4": "封面_竖.png",
    "4:3": "封面_横.png"
  },
  "tags": ["北海旅行", "避坑"],
  "preferredPlatforms": ["小红书", "微信公众号"],
  "platformOverrides": {
    "小红书": {"tags": ["北海旅行", "避坑"]},
    "微信公众号": {"title": "适合公众号阅读的标题"}
  },
  "originalDeclaration": false,
  "aiDisclosure": {
    "containsAiGeneratedContent": false,
    "contentKinds": [],
    "assetPaths": [],
    "allowPlatformAutoDeclaration": false
  },
  "debugDryRun": true,
  "publishAllowed": false
}
```

视频包将 `contentType` 改为 `video`，且 `assets` 必须只有一个视频文件；文字包将 `contentType` 改为 `text`，并省略 `assets`。

`covers` 至少要提供一张 3:4 或 4:3 封面。`preferredPlatforms` 仅用于导入后预选账号，仍可在客户端手动调整。

### 图文图片位置

图文包中的每张正文图片可以继续使用旧格式路径字符串，也可以使用位置对象：

- `path`：内容包内的图片相对路径。
- `placement: after_intro`：放在导语之后、第一节之前；单张旧格式图片默认使用此位置。
- `placement: before_section_1`：明确放在第一节标题之前，与 `after_intro` 使用同一安全落点。
- `placement: after_heading`：放在指定章节标题之后，必须同时填写 `anchor`，其值为标题文本。

旧内容包不需要修改：没有 `placement` 时，第一张正文图默认放在导语后；多图中的其余图片会按文章章节均匀分布，不会统一追加到文末。显式 `after_heading` 锚点优先于自动分布。

封面只从 `covers` 读取，不要把封面图片重复写进 `assets`。公众号预检若需要“从正文选择”设置封面，只会临时插入封面候选，完成选择后把该临时图片移出正文。

### 原创与 AI 声明

- `originalDeclaration` 只接受明确的 `true` / `false`。旧包缺省时按 `false` 处理，一键发不会根据文案风格或素材来源猜测原创权利。
- `aiDisclosure.containsAiGeneratedContent` 表示内容包是否有可核对的 AI 辅助依据，`contentKinds` 声明文字、图片、视频或音频类型，AI 图片还必须写入 `assetPaths`。
- 只有 `allowPlatformAutoDeclaration=true` 且存在上述 AI 依据时，客户端才能自动开启平台 AI 声明。该值为 `false` 时，必须由用户在本次任务中亲自勾选确认。
