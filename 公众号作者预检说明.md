# 公众号作者预检

一键发提供独立的“仅作者选择”预检通道，用于在完整图文预检之外单独验证作者控件。该通道不会填写标题或正文，不处理封面和正文图片，也不会定位或点击保存草稿、预览、发表及账号设置控件。

## 调用约定

调用 `app_core.oneclick_preflight.run_wechat_author_preflight_sync(payload)`，载荷至少包含：

```json
{
  "type": 10,
  "contentType": "article",
  "accountList": ["一键发本地公众号会话文件名"],
  "preflightOperation": "wechat_author_only",
  "runtimeMode": "preflight",
  "debugDryRun": true,
  "publishAllowed": false,
  "backgroundMode": false
}
```

账号会话文件名由一键发账号管理内部传递，不写入内容包，也不在外部文档中保存 Cookie 或登录信息。
该通道一次只接受一个公众号账号；多账号、空账号或非图文类型会在启动浏览器前拒绝执行。

## 选择规则

1. 只识别已知的公众号作者字段容器、组合框和作者选项列表。
2. 作者控件或列表必须唯一且实际可见；出现多个候选时不会猜测。
3. 默认选择当前账号作者列表中的第一个可见、可点击项。
4. “添加作者”“作者管理”“去设置”等入口永远不作为作者选项。
5. 不硬编码作者姓名；选择后必须从作者控件回读同一显示名。

## 安全停止

出现以下任一情况都会抛出预检错误并结束：

- 作者控件不存在、不唯一或页面结构发生变化；
- 作者列表没有加载、出现多个可见列表；
- 当前账号没有可用作者；
- 第一个作者选项被禁用、名称为空或属于管理入口；
- 点击后作者控件回读为空或与选中项不一致。

此通道的成功只表示作者字段已选择并回读，不表示平台草稿已创建或文章已发布。
