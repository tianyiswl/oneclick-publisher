# 一键发 UI 与蚁小二能力适配

## 当前边界

- 前台：直接复用发射台的 PyQt 页面、布局和样式。
- 后台：每个平台建立一键发自己的内置适配器；恢复代码只作为审计参考。
- 小红书：统一入口为 `app_core/xhs_native_adapter.py`，不再经过旧发布器、
  蚁小二客户端、`yxer`、蚁小二网关或私有签名服务。

## 逐步接入顺序

1. 将恢复代码的任务模型、字段限制和本地 RPA 顺序重写为一键发模块。
2. 使用一键发账号会话完成官方页面 dry-run，并逐字段回读。
3. 平台逐个建立“账号能力、草稿能力、人工确认、平台回执”的状态映射。
4. 只有获得明确授权和真实平台证据后，才启用相应平台的正式执行能力。

## 小红书固定策略

小红书图文和视频均由一键发内置执行器处理。预检绝不定位或点击最终按钮；正式
任务必须经过用户可见确认，并且只有获得小红书成功页、成功提示或平台内容 ID
等可验证回执后，状态才可以写为“已发布”或“已定时”。
