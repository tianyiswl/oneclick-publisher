# 一键发 UI 与蚁小二能力适配

## 当前边界

- 前台：直接复用发射台的 PyQt 页面、布局和样式。
- 后台：新建 `app_core/yixiaoer_adapter.py`，作为后续蚁小二能力的唯一入口。
- 现阶段：只允许本地字段校验、准备记录和 dry-run；没有任何外部请求。

## 逐步接入顺序

1. `doctor -> config get -> accounts list`：只读核验本机通道与账号能力。
2. `validate -> publish --dry-run`：将一键发发布中心的内容字段映射到蚁小二校验结构。
3. 平台逐个建立“账号能力、草稿能力、人工确认、平台回执”的状态映射。
4. 只有获得明确授权和真实平台证据后，才评估某个平台的真实执行能力。

## 小红书固定策略

小红书的适配器策略是 `manual_final_confirmation`：一键发只准备字段和待办，真人必须在官方页面完成最终确认；未获得可验证的平台回执时，状态不得写为“已发布”。
