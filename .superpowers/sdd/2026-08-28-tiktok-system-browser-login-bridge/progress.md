# SDD ledger — plan: docs/superpowers/plans/2026-08-28-tiktok-system-browser-login-bridge.md

## Setup

- Workspace: `/Users/andy/Documents/Codex/2026-07-28/new-chat/outputs/一键发桌面UI基座/.worktrees/overseas-login-publish-v2`
- Branch: `feature/overseas-login-publish-v2`
- Plan baseline: `3c5d786`
- Spec: `docs/superpowers/specs/2026-08-28-tiktok-system-browser-login-bridge-design.md`
- Isolation: linked git worktree; not a submodule; no additional worktree created.
- Baseline: `QT_QPA_PLATFORM=offscreen ../../.venv/bin/python -m unittest discover -v` — 2389 tests, 0 failures, 83.111s.

## Pre-flight consistency scan

| Scope | Producer / requirement | Consumer / test | Finding |
| --- | --- | --- | --- |
| Task 1 self-check | TikTok-only domain/origin filter and stable errors | Allowlist, malformed input, missing session, immutability tests | Consistent after adding malformed-port rejection. |
| Task 2 self-check | System browser discovery, owned staging, lifecycle and cleanup | Command allowlist, process outcomes, path/symlink and cleanup tests | Consistent; browser process fakes prevent real launches. |
| Task 3 self-check | Two-context handle readback and atomic account/session persistence | Leakage, mismatch, transaction, compensation and permissions tests | Conflict found: candidate collector text referenced `existing_account` absent from its signature. |
| Task 4 self-check | Public TikTok route, UI lifecycle and GUI-only stale cleanup | Routing, queue, source-live isolation and startup-order tests | Consistent; shared files are isolated in a separate commit. |
| Task 5 self-check | Regression plus one login-only source-client acceptance | Same-handle restart readback and TikTok-only structure | Consistent; explicitly forbids upload/form/Post. |
| Tasks 1 → 3 | `sanitize_tiktok_storage_state` | Candidate collection consumes sanitized state | Interface names and error contract match. |
| Tasks 1 → 5 | Scope tests and structural predicate | Real acceptance records only `tiktokOnly=true/false` | Evidence does not expose cookie names or values. |
| Tasks 2 → 3 | `SystemBrowserSpec`, `TikTokLoginAttempt`, process/profile-release lifecycle | `overseas_tiktok_system_login.py` candidate/session orchestration | Same file is intentionally extended; Task 3 starts after Task 2 review. |
| Tasks 2 → 4 | `recover_stale_tiktok_login_attempts` | GUI startup invokes recovery before `MainWindow` | Interface matches; CLI/MCP remain outside this helper. |
| Tasks 2 → 5 | Owned staging cleanup guarantees | Real acceptance checks staging removal | Evidence boundary matches design. |
| Tasks 3 → 4 | `TikTokSystemBrowserLoginSession` and stable queue protocol | `login_service` and `login_dialog` | Interface and message set match after adding `OPENING_SYSTEM_BROWSER`. |
| Tasks 3 → 5 | Verified account/session commit | Restart silent readback | Same public-handle contract is reused. |
| Tasks 4 → 5 | Public UI route and source-live startup | Real account-management acceptance | No public route remains on `_browser_cookie_gen(6)`. |

Ruling: Keep `collect_validated_tiktok_candidate` account-agnostic; perform the existing-account identity check in session orchestration and repeat it transactionally in the account saver — this matches the spec's single identity contract and avoids adding UI/database state to the browser intake helper — if wrong, Task 3 would need a signature change and focused rework before routing.

## Task status

- Task 1: complete (commits `179cef0..ec6e0be`, review clean)
- Task 1 base: `179cef0f963fca17c3e4d613cf07a4bbdc9e5c6c`
- Task 1 verification note: reviewer could not independently verify the reported full-suite run from the diff; controller confirmed the report contains the exact command/result (`2395/2395`) and the pre-task baseline independently passed (`2389/2389`).
- Task 2 base: `ec6e0be`
- Task 2 review at `09871c4`: spec failed — Critical: cleanup can escape through a symlinked staging ancestor; Important: dangling Chromium lock symlink is treated as absent; Important: process exit between poll and terminate can escape the finite outcome contract.
- Task 2 fix round 1/5 (2 addressed, 1 open — higher ancestor symlink still permits external deletion; commits `09871c4..9184b5e`).
- Task 2 fix round 2/5 (1 addressed, 0 open; commits `9184b5e..037dbd0`).
- Task 2: complete (commits `ec6e0be..037dbd0`, review clean after 2 fix rounds)
- Task 3: in progress
- Task 3 base: `037dbd0`
- Task 3 review at `649cefe`: spec failed — Important: cancellation can finish while the owned process survives; Important: commit-failure cleanup suppresses unlink failures; Important: old-session reference check and unlink have a race.
- Task 3: minor (deferred): cancellation during the profile-lock wait may still start Playwright before the next cancellation check.
- Task 3 verification note: focused `64/64` output is clean; full-suite `ResourceWarning` lacks traceback and cannot be attributed to this diff. Recheck under Task 5 full-suite evidence.
- Task 3 fix round 1/5 (3 addressed, 0 open; commits `649cefe..eaf5652`).
Ruling: If the owned browser does not exit after bounded termination, do not clear ownership, do not delete its staging, and terminate the login with `tiktok_login_cleanup_failed` instead of `CANCELLED` — a live browser makes safe cleanup unprovable — if wrong, users may see cleanup failure rather than cancellation and need to close the dedicated window manually.
Ruling: If rollback cannot remove a newly written candidate/temp session file, surface `tiktok_login_cleanup_failed` rather than hiding it behind `tiktok_login_commit_failed` — sensitive orphan cleanup outranks the original database error — if wrong, the public error wording will be less specific to the database failure.
Ruling: Old-session deletion must perform its final reference check and unlink while holding a new SQLite `BEGIN IMMEDIATE` transaction after the account row already points to the new file — this closes the DB-writer race without deleting an old file before the replacement row is committed — if wrong, file I/O inside a short write transaction may temporarily delay another account update.
- Task 3: complete (commits `037dbd0..eaf5652`, review clean after 1 fix round; 1 deferred Minor remains for final triage)
- Task 4: in progress
- Task 4 base: `eaf5652`
- Task 4 review at `2aca66b`: spec failed — Important: `run_ui_test()` calls `create_main_window()` and therefore can recover/delete real staging before source-live conflict checks.
Ruling: Keep normal `create_main_window()` recovery enabled by default as the plan requires, add an explicit keyword to disable recovery only for `run_ui_test()`, and test the real `run_ui_test` call path — this preserves production startup recovery while keeping self-test read-only — if wrong, another future non-production caller could omit the opt-out and would need the same explicit guard.
- Task 4 fix round 1/5 (1 addressed, 0 open; commits `2aca66b..e73e808`).
- Task 4: complete (commits `eaf5652..e73e808`, review clean after 1 fix round)
- Task 5: in progress
- Task 5 base: `e73e808`
- Task 5 real-login round 1: macOS Chrome reported Keychain authentication/encryption failure; after the dedicated window closed, silent validation returned `tiktok_session_expired` and no account was saved.
- Task 5 review at `f2a0229`: spec failed — Important: the mock-keychain flag was gated only by platform/browser name and did not itself prove the profile was the program-owned private TikTok staging profile.
Ruling: Before adding `--use-mock-keychain`, the macOS Google Chrome command builder must fail closed unless the profile is the exact existing `login-staging/tiktok/<32hex>/chrome-profile`, contains no symlinked owned path component, and is POSIX mode `0700`; Windows, Linux, and Edge remain unchanged — this keeps the workaround inside an ephemeral private profile — if wrong, a future caller could apply the predictable mock keychain to a normal Chrome profile.
- Task 5 fix round 1/5 (1 addressed, 0 open; commits `f2a0229..527c67f`; independent re-review PASS).
- Task 5 real-login round 2: mock-keychain removed the prior Keychain/encryption errors, but macOS kept the dedicated Chrome process alive after the user closed its last window; after the controller terminated only that owned process, validation still returned `tiktok_session_expired` before any account row was saved.
Ruling: Replace the unreliable macOS “close the window” completion dependency with a TikTok-only `完成登录并保存` action that gracefully stops only the session-owned browser and then validates; natural process exit remains supported, cancellation still discards the attempt, and the action never means login success by itself — if wrong, users on macOS remain permanently waiting or may confuse completion with a verified account.
Ruling: TikTok identity readback must wait a short bounded period for the SPA profile link to stabilize and must distinguish a final login/challenge route from an authenticated Studio page that lacks a unique public handle; diagnostics may expose only the route class and stable error code, never query strings, DOM text, cookies, or account secrets — if wrong, a valid but slowly rendered session will be discarded or a selector regression will continue to be mislabeled as an expired login.
- Task 5 fix round 2/5 (macOS completion action plus bounded identity readback; commits `527c67f..357d263`; independent review PASS).
- Task 5 real-login round 3: while the TikTok window stayed open, clicking “完成登录并保存” returned `tiktok_login_cleanup_failed`; subsequent read-only inspection confirmed the session-owned Chrome process had exited, but one TikTok staging directory remained.
Ruling: The completion action has its own bounded, default five-second graceful-exit wait for the session-owned browser. It must never call kill or affect a normal Chrome process; an exit within that window continues the normal validation flow, while a process that remains alive still returns `tiktok_login_cleanup_failed` and preserves staging. Cancellation and the overall login timeout keep their existing safety behavior — if wrong, a normal delayed Chrome shutdown can be discarded before its session is read, or an unproven cleanup can be reported as a success.
- Task 5 fix round 3/5 (delayed owned-browser exit acceptance, no-kill preservation, and bounded completion wait; commit `45201e8`; TikTok focused `136/136`, affected overseas `132/132`, account UI `20/20`; no real browser, upload, publish, push, or merge run).
- Task 5 real-login round 4: the explicit completion action and five-second owned-browser exit both passed; the subsequent TikTok-only candidate check reached an authenticated Studio route but returned `tiktok_account_invalid` because the current Studio account shell did not expose a visible unique public-profile link. Staging cleanup completed safely; no account was saved.
Ruling: On an HTTPS TikTok `/tiktokstudio/` route only, permit one normalized public `@handle` anchor even when responsive or closed account-shell markup makes it invisible, but require the same sole handle on two reads. Login/challenge and ordinary TikTok routes still require a visible link; more than one normalized handle, visible or hidden, is always `tiktok_account_identity_ambiguous`. Hidden anchors never supply display text, and diagnostics retain only fixed public error codes/messages — if wrong, an account shell change could still stop a valid login, while this narrow route guard prevents hidden feed or login markup from silently binding an account.
Ruling: Silent saved-session validation must call `persist_tiktok_identity` exactly once with `(account_id, identity, allow_initial_bind=True)`; the regression test freezes the intended two positional arguments and prevents an extra identity value from converting later restart readback into a generic expired session — if wrong, a malformed call would be caught before account detection mislabels it.
- Task 5 fix round 4/5 (Studio-only hidden public-anchor compatibility plus saved-validation call-shape regression; product commit `aed244e`; identity `25/25`, TikTok focused `141/141` including system-login, integration and UI, affected overseas `132/132`; syntax and whitespace checks clean. All tests use local fakes; no real browser, platform page, upload, form, publish, push or merge run).
- Task 5 review round 4a at `aed244e`: spec failed — Important: the Studio-route guard still accepted HTTP and Studio authentication subroutes, so hidden template anchors on a login/challenge page could be mistaken for the account identity.
Ruling correction: hidden profile anchors are eligible only on the exact HTTPS TikTok canonical `/tiktokstudio/<workspace>` route family, never on HTTP, a bare Studio root, a mixed-case Studio prefix, or any later path segment containing `login`, `challenge`, `verify`, `captcha`, or `security` (case-insensitive; trailing slash and query do not alter the rejection). The unique two-read handle rule, visible-link fallback, and fixed non-sensitive diagnostics remain unchanged — if wrong, an unexpected Studio shell will fail closed instead of binding a hidden authentication-page template.
- Task 5 fix round 4a/5 (authenticated Studio route hardening; product commit `0285495`; identity `26/26`, TikTok focused `142/142`, affected overseas `132/132`; syntax and whitespace checks clean. All tests use local fakes; no real browser, platform page, upload, form, publish, push or merge run).
- Task 5 real-login round 5: the completion action, delayed owned-browser exit and staging cleanup passed, but the authenticated Studio page still returned `tiktok_account_invalid`; current Studio markup exposes no `/@handle` anchor at all. No account was saved, and no upload or publish page was used after this boundary was identified.
Ruling: Supersede the Studio-anchor route as the primary identity source. Both candidate contexts and saved-session validation open the read-only HTTPS TikTok homepage and parse only `script#__UNIVERSAL_DATA_FOR_REHYDRATION__ → __DEFAULT_SCOPE__ → webapp.app-context → user → uniqueId/unique_id`. The parser is non-recursive, accepts one normalized public handle only after two identical reads, rejects malformed/missing fields and conflicts, ignores homepage feed author anchors, and never reads or retains uid, secUid, Cookie values, display text, query data or full page diagnostics. Authentication routes still reject before identity extraction; legacy Studio anchor code remains compatibility-only — if wrong, a homepage contract change fails closed rather than binding an arbitrary feed or template account.
- Task 5 fix round 5/5 (homepage app-context public-username identity readback; product commit `6fee390`; identity/system/integration/UI `140/140`, complete TikTok ladder `146/146`, affected overseas `132/132`; syntax and whitespace checks clean. All tests use local fakes; no real browser, upload, publish, push or merge run).
- Task 5 review round 5a at `6fee390`: spec failed — Important: Python received the whole homepage rehydration script for `json.loads`, which could carry uid, secUid and nickname; the decode exception also retained the raw document as a cause.
Ruling correction: the browser page itself performs the fixed script lookup and `JSON.parse`, then returns only `{state, uniqueId, unique_id}` or a fixed `missing`/`invalid` state through `page.evaluate`. Python never receives script text or a parse exception; it validates only the two returned public fields, and page-side failures have no chained document/error cause — if wrong, identity extraction stops safely instead of crossing the session-data boundary.
- Task 5 fix round 5a/5 (page-side public-username extraction; product commit `e9fb1dc`; identity/system/integration/UI `140/140`, complete TikTok ladder `146/146`, affected overseas `132/132`; syntax and whitespace checks clean. The secret-sentinel fake proves no script content enters evaluate calls, identity values, public errors, reprs or causes. No real browser, upload, publish, push or merge run).
- Task 5 real-login round 6: after the user completed the dedicated macOS Chrome login and chose the explicit save action, validation ended with `tiktok_account_invalid` after about 18 seconds; the account count remained `11` and no account was written. Read-only structural inspection after cleanup found no owned attempt/profile available for reuse. The candidate collector reopened the same dedicated profile without the macOS Chrome mock-keychain flag used by the interactive launcher, so it could not be assumed able to decrypt the just-written login state. A separate read-only homepage check found the fixed rehydration script present while the document could remain `interactive` for several seconds, making a `complete` prerequisite unsafe.
Ruling: macOS Google Chrome candidate readback must use `--use-mock-keychain` only after the existing owned-profile guard has accepted the private staging profile; Edge, Windows and Linux keep their current command arguments. Homepage identity polling has one bounded 12-second window, invokes the existing page-side public-field extractor on every poll without waiting for `document.readyState == complete`, and still requires two matching normalized handles. No post-failure automatic reuse is permitted: without a verified public handle the owned temporary profile must be cleaned, and this run left no safe state to reopen.
- Task 5 fix round 6/5 (macOS Chrome reopen parity plus bounded interactive-homepage app-context polling; product commit `88507be`; red tests first for the missing reopen flag and for delayed/interactive context, then green. Identity/system/integration/UI `144/144`, complete TikTok ladder `150/150`, affected overseas `132/132`; no real browser, upload, form, publish, push or merge run).
- Task 5 review round 6a at `88507be`: spec failed — Important: the 12-second homepage window was not a hard deadline because the old loop checked it only after reaching `max_attempts`; a large poll interval could wait far beyond the declared limit.
Ruling correction: enforce the homepage deadline before and after every safe page-side extraction, and cap each wait at `min(poll_seconds, remaining)`. A positive interval may continue beyond the old five-read floor only until that deadline, while a zero interval retains the configured attempt bound for deterministic tests. A lone handle at the deadline remains invalid; two matching handles remain required — if wrong, an exhausted identity read can still hold the user beyond the promised window or bind from one transient value.
- Task 5 fix round 6a/5 (hard bounded homepage polling; pending commit; red tests proved `poll_seconds=10` slept four 10-second intervals under the old loop, then green with deadline-capped sleep. TikTok ladder `153/153`, affected overseas `132/132`; no real browser, upload, form, publish, push or merge run).
- Task 5 real-login round 7: after `7cbb09c`, the dedicated login and explicit completion completed, but local validation still ended at `tiktok_account_invalid` after about 28 seconds; the account count remained `11` and no account was written. The collector read page identity from the reopened persistent profile before it exported and filtered state, so this failure could not distinguish absent saved login state from a persistent-profile shell identity-source failure.
Ruling: the persistent profile is limited to `storage_state` export and TikTok-only sanitization, then is closed before any page navigation. A sanitized state must contain one of the fixed private login-session markers; a TikTok visitor-only state is `tiktok_session_missing`, never authenticated. Two independent blank contexts each open the homepage and read the existing public handle source; only equal unique handles may proceed. Candidate diagnostics retain only `auth_cookie_present` and a fixed stage code in memory. Missing/ambiguous blank identity, missing session, expired route and two-context mismatch have distinct fixed error codes and UI text, with no values, cookie names, URLs, DOM text or account data exposed — if wrong, the system fails closed without saving an account.
- Task 5 fix round 7/5 (persistent-state export plus two blank-context verification and auth-marker check; product commit `4a978f3`; red tests covered missing auth marker, no persistent page read, two blank contexts, first blank failure, ambiguous identity, resource cleanup and no commit, then green. TikTok ladder `155/155`, affected overseas `132/132`; no real browser, upload, form, publish, push or merge run).
