# 一键发能力迁移边界

一键发是唯一的运行客户端。账号授权、素材选择、发布准备、平台草稿、人工确认
与回执状态都应在一键发中发起、保存和展示。

蚁小二恢复工程只作为能力源码审计与移植来源。本项目不会在运行时：

- 启动或要求用户登录蚁小二客户端；
- 读取其账号、Cookie、用户目录或任务；
- 调用 `yxer` 命令行、云任务或原签名服务。

当前已移植到一键发本地能力层的是国内六平台内容类型矩阵、字段预检规则及
官方页面授权入口，分别见 `app_core/oneclick_capabilities.py` 和
`app_core/oneclick_authorization.py`。规则依据恢复包中的 Schema 和本地 RPA
适配约束：视频、图文、文字在不支持的平台会在创建任务前被拦截；B站和公众号
文章类内容会强制封面。授权浏览器资料和保存后的 storage state 都只属于一键发。

真实登录由用户在一键发打开的平台官网完成；保存会话后仍必须检测，不能直接标记
为“已发布”。小红书图文/视频的上传、字段回读、立即/定时发布已经收敛到一键发
内置执行器；真实页面兼容性仍须逐项预检。只有收到平台回执，任务才能标记为
“已发布”或“已定时”。
