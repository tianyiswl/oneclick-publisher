# 一键发项目协作规则

## 启动顺序

- 非简单任务开始前先读 `SOURCE_OF_TRUTH.md` 和 `QUALITY_GATES.md`。
- `SOURCE_OF_TRUTH.md` 记录当前版本、能力状态、真实验收和唯一下一步；源码、配置与同次运行证据决定当前实现事实。
- 旧计划、旧报告和旧截图只用于定位线索，不能覆盖当前状态表或最新平台回读。

## 三线隔离

- 数据监测、海外登录发布、国内定位属于同一个产品下的三条功能支线，必须使用独立 branch 和 worktree。
- `main` 只接收经过验证的集成结果，不直接进行功能开发。
- 每条支线只修改 `QUALITY_GATES.md` 列出的专属文件。共享核心文件只能在集成线修改；确需由功能线提出时，必须拆成独立提交，交由集成线审查和合并。
- 开始提交前运行 `tools/check_workstream_scope.py`。出现 `shared` 或 `outside` 文件时不得自行合并。
- 未跟踪文件属于用户或其他进行中的工作。没有先确认归属，不得删除、移动、覆盖或顺手提交。

## 状态和证据

- 严格区分：源码已接入、本地测试通过、真实登录成功、平台预检通过、已触发最终动作、平台明确成功、内容列表回读成功。
- 数据同步必须记录来源、采集时间、缺失字段和失败状态；未知值不得写成 `0`。
- 登录态、Cookie、二维码、验证码、数据库、账号资料和平台响应原文不得进入 Git、构建包或长期日志。
- 测试通过不等于真实平台可用；真实平台动作必须按 `QUALITY_GATES.md` 的分线验收标准单独取证。

## 内容包和平台字段

- 发布包以结构化 `manifest` 为权威输入。标题、正文、话题、提及、封面、AI 声明、定位和各平台覆盖必须分字段保存；带有“## 抖音/## 小红书”等预览结构的 Markdown 不得作为发布正文。
- 旧内容包只能显式迁移或给出清楚错误，不能静默拼接、补写或污染正文。
- 正文中的普通 `#文字`、`@文字` 不能冒充平台话题或提及。只有点选官方候选并回读到平台实体后才算写入成功；没有独立字段与实体回读时必须停止。
- 预检与正式发布必须调用同一套字段填写和回读逻辑。预检成功的条件不得比正式发布宽松。

## 浏览器会话和页面变化

- 音乐刷新、地点搜索、数据同步和发布使用受控会话：按需创建，完成、失败、取消、放弃本次上传或一批任务结束后都要关闭页面与临时会话，并清除仅属于本轮的内存状态。
- 第二批任务、暂停后继续、放弃后重来都必须从明确初始状态开始；不得复用仍打开的下拉框、搜索面板、旧候选或旧自动填充值。
- 平台页面变化时先用当前可见、可交互控件取证，再修改选择器；不能把旧选择器命中、文字存在或按钮点击成功当作字段已生效。
- 用户已经在平台页面手动修改的内容默认只读。除非用户明确要求，不再打开、覆盖或重复提交该页面。

## 定位搜索和缓存

- 地点缓存键至少包含账号、平台、搜索范围、筛选条件和完整搜索词；`夜南香`、`广东夜南香`、`广州夜南香` 必须是互不污染的缓存。
- 更换搜索词时，保留用户手动选择的地点；清除旧搜索自动填入的地点。新候选不足时，多出来的视频保持“请选择地点”，不得回填其他地区旧缓存。
- “加载更多”必须真实滚动到平台候选面板底部、点击平台按钮、等待新增并回读；本地缓存已含前几页不能被误判为平台没有更多。平台明确无入口或无新增时才停止。
- 因地点错误导致正式失败的候选要标记为待重新核验，下次不能直接按有效缓存使用。

## 受控发布和任务终态

- 桌面 UI、CLI 和本地接口必须调用同一个发布服务；任务输入固定为内容包、明确账号、执行模式和各平台发布时间，不接受 Cookie、密码或验证码。
- 默认只允许预检。正式发布必须绑定本次全部成功的预检，并消费短期、一次性授权；已消费授权不能重用。
- 一个平台失败不能让其他平台永久 `pending`。进程中断、线程异常或客户端退出后，任务必须由租约、恢复或清理逻辑进入明确终态，并返回稳定错误码。
- 任务结果至少区分预检成功、正式动作已触发、平台受理、定时回执和公开可见；缺少后一层证据时不得升级状态。

## 测试节奏

- 先复现或增加失败测试；发现第一个真实问题后停止扩大测试范围，先修复并跑最相关单项测试。局部稳定后再跑受影响模块，收尾时才跑全量测试和离屏客户端。
- 真实平台验证优先单账号、单视频、单功能；没有当前能力账号时可以完成源码和自动测试，但必须明确标为“等待用户真实账号验证”。

## 版本和发布

- 功能分支不修改应用版本号，也不制作正式安装包。
- 只有集成线准备合并发布时统一递增版本，并核对应用界面、Mac 包、Windows 包名和构建产物版本一致。
- 每次对外提供新的客户端更新都必须自动递增版本；旧版本号不得覆盖新包。Git 发布区默认只保留最新候选包，清理旧包前仍需确认准确目标和可恢复性。
- 源码修复、Git 提交和客户端打包是三个不同结果。没有重新打包并安装时，现有客户端不包含最新源码修改。
- 没有平台回执时，不得把上传、按钮点击、等待超时或本地任务成功写成已发布。
