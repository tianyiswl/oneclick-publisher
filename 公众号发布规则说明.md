# 一键发公众号发布规则

## 默认行为

- 新建公众号发布任务默认开启 `wechatGroupNotification=true`（群发通知）。
- 只有用户在公众号平台适配页明确取消“开启群发通知”，才传入
  `wechatGroupNotification=false`。分组通知不由一键发独立猜选，遵从公众号
  对群发通知父选项的联动。
- 未勾选“定时发布”时传入 `enableTimer=false`、`scheduleTime=null`，保持
  立即发表；一键发不得因字段残留自行切换成定时发表。
- 勾选定时后，`scheduleTime` 必须是用户本地时区中的未来时间，同时附带
  `scheduleTimezone`。执行器必须回读平台的“定时发表”状态和分钟级时间，
  任一不一致均停止。
- 未勾选“原创声明”时传入 `originalDeclaration=false`，作者流程完全跳过：
  不读取、不打开、不填写、不清空、不选择作者。
- 只有明确勾选原创时，才打开作者控件、选择当前账号列表中的第一个可用
  作者并回读。作者为空、列表失败、控件变化或回读不一致均停止。

## 可自动接受的精确确认

- 当且仅当用户已开启群发通知，平台弹窗的标题、正文与按钮完整匹配已确认
  版本时，一键发可自动点击“继续发表”：
  - 标题为“发表”；
  - 正文只说明内容将展示在用户的公众号列表和公众号主页，以及允许平台推荐
    时可能进入看一看或其他推荐场景；
  - 按钮只能是“查看详情”“继续发表”“取消”。
- 文案、标题或按钮任一变化，或出现协议、法律、风控、违规、时事、公共政策、
  社会事件、账号设置、管理员验证等额外内容，必须停止并交由用户确认。
- AI 创作来源声明仍只在内容包元数据明确标记相符 AI 素材时自动接受。
- 二维码永不自动处理，必须由用户在一键发原生验证窗口中扫码。

## 后台微信验证协议

公众号执行器遇到微信二维码验证时，应在同一发布线程、同一浏览器上下文中
调用 `app_core.wechat_verification.request_wechat_verification()`：

1. 二维码以 PNG/JPEG 字节传入，只保存在当前进程内存。
   执行器会校验图像尺寸、近方形比例与二维码暗像素覆盖率；空白容器、提示
   文字或损坏图片不得进入弹窗。
2. 任务事件只记录“需要验证、成功或停止”，不得记录二维码、URL、Cookie、
   令牌或账号凭据。
3. 一键发前台弹出“需要微信验证”，展示等待、验证中、过期、取消、成功或
   失败状态。
4. 二维码过期时只调用执行器提供的刷新回调；刷新失败即安全停止。
5. 扫码成功后，等待函数在同一个请求上返回，执行器从原调用点继续同一发布
   会话，不新建浏览器上下文。
6. 浏览器不会自动前置；只有用户点击“打开验证页面”时才调用执行器提供的
   备用显示回调。
7. 扫码导致平台跳转时，执行器应有界等待新页面上下文稳定；若平台直接回到
   首页，可通过标题关联的“定时发表”卡片回读日期、时间和群发通知状态，
   不得因瞬时成功提示消失而重复提交。

该交互不改变最终发表、AI 来源声明匹配、群发、定时或原创作者的判断规则。
除上文两类精确匹配声明外，任何新合规提示、风控、账号异常或不可逆选项仍
必须停止。
