# 微信公众号内容包

在“发布中心”选择“导入公众号内容包”，然后选择内容包中的 `manifest.json`。

导入只会把标题、正文和封面带入编辑器，并强制保持“预发布检查”。它不会上传、保存草稿或发表。

## 目录结构

```text
我的文章/
├── manifest.json
├── 正文.md
└── cover.png
```

`cover.png` 当前需要使用 3:4 或 4:3 比例。正文文件必须使用 UTF-8 编码。

## manifest.json

```json
{
  "schemaVersion": "oneclick-wechat-content/v1",
  "platform": "微信公众号",
  "contentType": "text",
  "title": "文章标题",
  "bodyFile": "正文.md",
  "cover": "cover.png",
  "debugDryRun": true,
  "publishAllowed": false
}
```

也可以不用 `bodyFile`，改为在 `body` 字段中直接写正文；两者只能选一个。

包内不得保存账号 Cookie、登录信息或自动发表配置。`debugDryRun` 必须为 `true`，`publishAllowed` 必须为 `false`。
