# YouTube 官方私密发布验收（2026-08-27）

## 验收范围

- 账号：`YouTube测试 | Mobai`
- 频道：`UCig…kZw`（完整公开频道 ID 仅保存在本机账号库）
- 标题：`一键发 YouTube 私密通道验收 2026-08-27`
- 目标设置：`private`、非儿童内容、关闭订阅者通知
- 视频 SHA256：`7ea9be2cf5950f273b959801a21052eec29f5c37d8f554ebe4498148e7212219`
- 封面 SHA256：`f6050d60b08a0d60e0ac29747a9b1638eabf9927267886eaf53efda9d3aab86a`

## 真实执行与回读

1. 零写入预检任务 `55 / T08272337-777A` 成功，回执为
   `platformMutation=none`、`visibility=private`，没有视频 ID。
2. 大帅看到账号、频道、标题、素材哈希和私密设置后，单独明确授权一次私密上传。
3. 正式任务 `56 / T08272339-B567` 只执行了一次。YouTube 于
   `2026-08-27T15:39:51Z` 创建视频，但客户端把平台有效的 HTTP 200 完成响应
   错当成结果不明，任务最初记录为 `youtube_upload_outcome_unknown`。
4. 没有重试或再次上传。随后只读频道上传列表找到唯一匹配的目标视频
   `FzC…A9k`，并用该精确视频 ID 回读：频道匹配、标题匹配、
   `privacyStatus=private`、`madeForKids=false`、`uploadStatus=processed`、
   `processingStatus=succeeded`。
5. 因误判发生在设置封面之前，系统在再次核对精确视频 ID、频道、标题和私密状态后，
   只给该视频补上原验收封面；缩略图接口返回成功，随后同 ID 回读仍保持私密和非儿童内容。
6. YouTube Studio 的精确视频页面再次显示同一标题、3 秒时长、非儿童内容和
   `Visibility: Private`。没有编辑可见性，也没有创建第二条视频。

## 根因与修复

- 根因：断点续传完成逻辑只接受 HTTP 201；YouTube 本次以 HTTP 200 和有效视频 ID
  返回成功，客户端因此误报结果不明。
- 修复提交：`b227dd0`，完成响应现在同时接受 HTTP 200/201，但仍要求响应包含有效视频 ID，
  后续继续只按该精确 ID 回读。
- 回归：新增 HTTP 200 完成响应测试；YouTube API 与正式发布模块共 `51/51` 项通过。
- 安全检查：正式数据 SQLite 和日志中的令牌、刷新令牌、授权码与带授权码回调模式命中均为 0。

## 结论与边界

已经验证一次 Google 官方 OAuth/API 私密上传、精确视频 ID 回读、封面设置和 Studio
页面回读。正式任务 `56` 保留最初的失败记录，本文记录后续人工对账事实，未把旧失败记录
静默改写成原始成功回执。

这只证明私密发布通道真实可用。公开、不公开和定时公开仍只有本地自动测试，不能写成真实
平台验收通过。
