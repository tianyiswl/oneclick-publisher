# 国内平台数据监测：真实只读验收记录（2026-08-21）

## 本次结论

公众号已在同一登录态下真实连续两次完成只读同步，并完成 SQLite 与离屏页面读回。两轮
均为部分成功：官方作品列表只提供最近 10 条，系统如实标记为 `content_list_truncated`，
没有将其写成全量作品数据。视频号、B站和快手仍维持原有阻塞状态。

## 账号可用性（脱敏）

公众号本次使用当前客户端真实运行库中的 `id=20/type=10` 账号。一次性验收进程在导入
业务模块前固定运行库根目录，确认数据库指向主客户端运行库且账号可列出；业务代码仍
来自当前 worktree。未记录显示名称、会话文件信息或任何会话内容。

## 逐平台结果

| 平台 | 验收状态 | 脱敏依据 | 本次同步与读回 | 解除条件 |
| --- | --- | --- | --- | --- |
| 公众号 | 真实连续两次通过 | 当前 worktree 代码在主客户端真实运行库中顺序执行。两轮均使用 `browser_signed`，仅出现受控警告 `content_list_truncated`，表示最近作品列表不完整而非同步失败。 | run `15`、`16` 均为 `partial_success`；每轮 72 个指标、10 条作品，账号/指标/内容所有权匹配，最新读回对应 run `16`，观察时间非空。内容总数 10、可用性为部分、警告为 `content_list_truncated`。成功返回合同确认每轮浏览器资源 `aliveResourceCount=0`。 | 当前无需解除阻塞；后续若要全量作品，须先取得已审核的分页合同。 |
| 视频号 | 未通过：collector_not_available | 官方站点导航在发生前即被安全策略拦截，未读取页面、登录态或任何平台响应；尚未取得官方数据合同，采集器未注册。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在获准进行该官方站点只读导航的环境中，确认账号摘要、内容列表和内容指标合同。 |
| B站 | 未通过：login_required | 创作中心显示登录提示或进入官方登录页；没有该平台的真实账号绑定，未观察到账号或稿件数据合同。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在一键发中重新绑定并登录 B站账号后，只读确认账号统计、稿件列表和稿件指标合同。 |
| 快手 | 未通过：login_required | 官方账号检查只呈现登录入口形态，未返回账号身份；未观察到作品列表或作品指标合同。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在一键发中重新登录并保存快手真实账号后，只读确认账号身份、作品列表和作品指标合同。 |

## 未宣称的验收项

对视频号、B站和快手，以下项目均未发生，故不以本地或离线结果替代：

- 连续两次真实同步；
- 同步运行的 SQLite 读回、账号/内容归属校验与不同运行 ID 校验；
- `browser_signed` 来源模式和平台观察时间校验；
- 浏览器资源清理回执；
- 真实客户端中平台选择保持、账号卡片刷新和内容行刷新。

公众号已完成两次真实同步、SQLite 读回和一次离屏页面读回；未确认的点赞、评论、分享
等字段仍维持为 `null/平台未提供`，没有记作零。

## 依据与后续

本记录依据本轮四份只读探测报告及本次公众号真实运行库读回：公众号已完成两次受控
同步，视频号被安全裁定禁止导航、B站会话失效且无真实账号、快手为 `login_required`。
本轮没有再次尝试规避、重放或重复探测仍受阻的平台。

其余平台重新具备解除条件后，仅对该平台重做：一次受控只读合同确认、两次真实同步、
SQLite 读回和一次真实界面刷新检查；任一步失败即停止该平台，不继续重复请求。
