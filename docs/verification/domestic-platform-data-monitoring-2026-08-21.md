# 国内平台数据监测：真实只读验收记录（2026-08-21）

## 本次结论

本次四个平台均未形成真实连续两次通过。公众号虽已有已审查采集器，但当前正常运行库
未提供指定的真实账号；第一次 `sync_account_data(20)` 在打开采集器前即失败。因此没有
写入同步记录或指标快照，也没有启动数据监测页的真实同步。其余平台仍维持原有阻塞状态。

## 账号可用性（脱敏）

本记录仅汇总已有只读探测结论和本次受控本地读回。当前正常运行库中不存在指定的
`id=20/type=10` 账号；仅发现另一条 `type=10` 账号行，不能替代指定账号进入真实验收。
未记录显示名称、会话文件信息或任何会话内容。

## 逐平台结果

| 平台 | 验收状态 | 脱敏依据 | 本次同步与读回 | 解除条件 |
| --- | --- | --- | --- | --- |
| 公众号 | 未通过：collector_not_available | 已审查采集器已注册，但当前正常运行库不存在指定的 `id=20/type=10` 账号。第一次 `sync_account_data(20)` 在浏览器启动前返回固定错误码；未发生平台请求。 | 第一次：`failed / collector_not_available`，运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权和清理回执均为 `null/未产生`；第二次和 UI：按失败停止，未执行。 | 将真实 `id=20/type=10` 账号写入当前正常运行库并确认一键发可列出该账号后，重新从第一轮开始验收。 |
| 视频号 | 未通过：collector_not_available | 官方站点导航在发生前即被安全策略拦截，未读取页面、登录态或任何平台响应；尚未取得官方数据合同，采集器未注册。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在获准进行该官方站点只读导航的环境中，确认账号摘要、内容列表和内容指标合同。 |
| B站 | 未通过：login_required | 创作中心显示登录提示或进入官方登录页；没有该平台的真实账号绑定，未观察到账号或稿件数据合同。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在一键发中重新绑定并登录 B站账号后，只读确认账号统计、稿件列表和稿件指标合同。 |
| 快手 | 未通过：login_required | 官方账号检查只呈现登录入口形态，未返回账号身份；未观察到作品列表或作品指标合同。 | 两次同步：未执行；运行 ID、内容数、指标数、来源模式、观察时间、SQLite 所有权：均为 `null/未产生`；浏览器清理回执：未产生。 | 在一键发中重新登录并保存快手真实账号后，只读确认账号身份、作品列表和作品指标合同。 |

## 未宣称的验收项

以下项目均未发生，故不以本地或离线结果替代：

- 连续两次真实同步；
- 同步运行的 SQLite 读回、账号/内容归属校验与不同运行 ID 校验；
- `browser_signed` 来源模式和平台观察时间校验；
- 浏览器资源清理回执；
- 真实客户端中平台选择保持、账号卡片刷新和内容行刷新。

四个平台均未提供的字段维持为 `null/平台未提供`，没有记作零。

## 依据与后续

本记录依据本轮四份只读探测报告及本次公众号运行库读回：公众号当前为账号行缺失，
视频号被安全裁定禁止导航、B站会话失效且无真实账号、快手为 `login_required`。本轮
没有再次尝试规避、重放或重复探测这些状态。

重新具备某个平台的解除条件后，仅对该平台重做：一次受控只读合同确认、两次真实同步、
SQLite 读回和一次真实界面刷新检查；任一步失败即停止该平台，不继续重复请求。
