# 硅基进化公众号只保存草稿桥验证记录

日期：2026-08-25（Asia/Shanghai）

## 本轮范围

- 输入只允许硅基进化 V1.2 `release_ready` 冻结包，且必须明确 `publish_allowed=false`。
- 一键发只允许保存公众号草稿，并在草稿列表按账号、标题、封面和保存时间窗口回读唯一结果。
- 禁止发表、群发、定时发表；内容项目的原文章状态不因草稿回执改变。

## 已实现的离线边界

- 冻结包会重新校验整包哈希与清单中每个文件的哈希，路径越界、素材变化或发表授权开启都会停止。
- 冻结 HTML 保持原样式和正文顺序，只移除本地图片占位并按正文锚点上传对应图片；摘要也必须写入后精确回读。
- `wechat_draft` 是独立运行模式，只接受单个公众号账号，并强制关闭群发和定时配置。
- 保存草稿后重新确认当前账号，再进入草稿列表；只有本次时间窗口内同标题、带封面的唯一结果才算成功。
- 本地收件箱、并发锁和回执使用原子文件；同一文章和同一冻结包已经有终态回执时不会重复执行。
- 桌面开关默认关闭，首次开启前明确说明只保存草稿；关闭后不轮询收件箱。
- 内容项目只登记文章 ID 与冻结包哈希完全匹配的回执，不改变本地交付或人工发表状态。

## 自动验证

- 一键发草稿桥本轮直接受影响模块：`58/58` 通过。覆盖冻结包可见文本、队列模板、草稿按钮新旧文案、首页近期草稿、CSS 封面、原生扫码窗口、桌面开关和服务路由。
- 硅基进化内容侧：`test_draft_bridge.py` 为 `6/6` 通过，`test_cli_draft_bridge.py` 为 `1/1` 通过；匹配回执只登记独立证据，文章仍保持原状态。
- 离屏客户端：`QT_QPA_PLATFORM=offscreen .venv/bin/python desktop_native_app.py --ui-test` 返回 `NATIVE_DESKTOP_UI_OK`。
- 空白检查：`git diff --check` 通过。
- 完整套件本轮共 `1910/1910` 项通过，用时 `67.904s`。离屏客户端返回 `NATIVE_DESKTOP_UI_OK`，`git diff --check` 通过。

本轮一键发源码提交：

- `d68ce1f`：草稿列表唯一回读。
- `3a03730`：默认关闭的桌面收件箱、原子回执和幂等锁。
- `0a6398a`：冻结 HTML、正文图片锚点和摘要回读。
- `e52b80e`：草稿任务原生扫码暂停与同会话恢复。
- `ab467e9`：允许超时发生在浏览器尚未创建时的正确资源测试口径。
- `9cae7c5`：用可控 CPU 负载验证评论 JSON 总截止时间，避免把操作系统首次线程调度误判为业务失败。
- 本提交：接通正常桌面启动入口，并兼容当前公众号模板、保存按钮、冻结正文空白和首页“近期草稿”回读。

内容侧提交：

- `5703f7e`：交接与匹配回执证据。
- `bb796e2`：日常任务接线、说明文档和子模块指针。

## 第一次真实验收结果

- 输入：`WX-20260825-002`，冻结包 SHA256 为 `bb6c0285f4ecdb20f4bcb0a1f63a306c81f5864c2990e1f77c175ed878d70589`，目标账号为“硅基进化”。
- 前三次任务在点击保存前分别暴露并修复了：正常启动未接入草稿目录、队列模板名不受支持、冻结 HTML 与 DOM 块级空白回读不一致，以及后台按钮从“保存草稿”改为“保存为草稿”。这些失败任务均保留为失败证据。
- 任务 `T08251309-FB1D` 已点击“保存为草稿”。保存后旧代码仍寻找“草稿箱”，因此任务先记为 `draft_readback_missing`；此时没有盲目重试保存。
- 随后的只读核验在公众号首页“近期草稿”唯一找到目标标题，页面显示“更新于 13:10”，CSS 背景封面存在。修复后的当前回读代码返回 `ok=true`。
- 原子回执已写为 `draft_readback_confirmed`；硅基进化内容项目已记录 `wechat_draft_readback_confirmed`，文章仍保持 `delivered_unverified`。
- 本轮平台没有要求扫码；没有点击“发表”，没有群发，没有定时发表，也没有公开链接。结论只能写为“真实草稿已保存并回读”，不能写为“已发表”。

## 仍保留的客户端验收边界

真实保存和平台回读已完成，但最终成功任务是在 Mac 锁屏后从同一条本机草稿队列直接启动；此前桌面开关已完成真实开启和失败任务接线验证。下一版客户端更新后，应使用下一篇新内容再从桌面开关启动一条完整成功链路；不得为补证重复保存本篇草稿。
