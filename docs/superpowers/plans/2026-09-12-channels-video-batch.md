# 视频号批量视频实施计划

执行方式：当前集成工作树内顺序实施。设计：`docs/superpowers/specs/2026-09-12-channels-video-batch-design.md`。

目标：单账号多视频、通用内容加逐字段覆盖、明确排期、独立回执与去重。

- [x] 内容合同：新增 `app_core/video_batch_contract.py` 和 `test_video_batch_contract.py`。验证空覆盖、继承、素材哈希、稳定身份、间隔/每日排期、过期时间与重复素材。`prepare_batch(request, now=...) -> dict` 返回冻结内容与 snapshotHash。
- [x] 草稿保存：新增 `app_core/video_batch_draft_service.py`，白名单 JSON、原子替换，未填写完成的草稿可恢复；保存不触发发布。
- [ ] 任务执行：持久化批次、条目、授权、提交意图、租约和结果；最终动作前写入意图，崩溃后禁止重放。UI/CLI 共用服务。
- [ ] 视频号适配：正文和短标题独立传递，提交只点击一次；当前页面官方话题、封面、身份与成功回执须真实取证后实现。没有回执能力不得开放批量正式发布。
- [ ] 页面与 CLI：增加发布中心批量入口，视频选择、排序、覆盖、排期、草稿与结果；CLI 本地检查与查询。
- [ ] 回归：最相关测试、受影响模块、完整离线发现、离屏客户端、git diff 检查；真实平台只在具体素材和账号授权后验收。

验收例：通用话题变更不覆盖用户清空的话题；排序不改变 itemId；相同文件改名被识别为重复；固定时段跨日正确；点击后超时不再次点击；已成功任务不能重放；源文件变化使冻结快照失效；已定时不等于公开可见。

## 2026-09-12 实施进展

页面内容准备与 CLI `batch-check` 已接线；全量回归 3207 项通过（后续新增用例另行定向验证）。本地独立队列、租约、授权、提交意图及单次提交边界已完成单元测试，尚未接入实际后台执行和既有任务记录。`formalAvailable=false`，没有授权/正式发布 CLI 入口。

当前仍需完成：视频号当前页面的身份、官方话题实体、封面及唯一作品回执适配；队列执行/暂停/续跑及任务记录接线；正式 UI/CLI 授权；真实小批次验收。旧 TencentVideo.click_publish 循环仍存在，尚未替换，不得宣称旧通道已修复。

本机只读账号查询发现唯一视频号账号 id=5、本地 status=0；这只是本地状态，不是实时登录结果。下一真实步骤是恢复/核验账号登录，并确定测试视频后仅做表单预检。未获得具体内容授权不得正式发表。

## 续接边界

任务记录接线后续：源码 TaskPage 新增“视频号本地批次”入口，直接读 BatchQueue 的安全投影，不复制通用任务库；列表与详情、取消待执行、错误保留与批次归属测试已完成。正式适配器和续跑依旧待完成。

离线续接已实现：`video_batch_executor.py` 通过模拟适配器串行执行；共享服务/CLI 本地生成、查询、取消批次；查询回收过期租约；逐条结构化原因；草稿类型和窗口关闭保护。最终定向 45 项通过。任务执行总项仍未勾选：通用任务列表、真实平台适配及正式续跑仍缺，不把局部交付标为整个功能完成。

用户重新登录后，本地账号 id=5 已回读 status=1；这替代上面的历史 status=0，但不代表发布验收。随后通过客户端打开后台时，Computer Use 明确禁止访问当前 URL；停止该页面的自动化，不得改用其他通道规避。用户现已授权继续离线开发与测试。先补本地队列执行、查询、取消及故障回归，正式能力保持关闭；平台字段与回执验收仍待可用的合规验证方式和具体素材授权。
