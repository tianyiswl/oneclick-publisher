# 一键发 0.5.24 Windows 构建验证记录

日期：2026-08-29（Asia/Shanghai）

## 本次交付

- 源码：`main@8e8d6f6549ab5fa6e2d3a02d63d9c73ed4ccf0d5`。
- GitHub Actions：运行 `33257050594`，结论 `success`。
- 候选标签：`v0.5.24-candidate-20260829`。
- 文件：`YiJianFa_0.5.24_Windows_x64_20260829.zip`。
- 文件大小：`444121170` 字节。
- SHA256：`288daecfdefc2ff3b45b2aeded2ef970b22cb0aa6a7e5d8e6281757881db0942`。
- 本地交付：`/Users/andy/文件/一键发/YiJianFa_0.5.24_Windows_x64_20260829.zip` 及同名 `.sha256` 文件。

## 验证结果

- Windows Runner 源码离屏界面返回 `NATIVE_DESKTOP_UI_OK`。
- 打包后的 Windows 主程序返回 `NATIVE_DESKTOP_UI_OK`，内置浏览器返回 `NATIVE_DESKTOP_BROWSER_OK`。
- 构建脚本返回 `WINDOWS_BUILD_OK`，包名中的版本为 `0.5.24`。
- 从候选版本重新下载后，SHA256 与 Runner、GitHub 资产摘要和校验文件一致。
- ZIP 完整性检查通过，共 `1972` 个文件；主程序为 `YiJianFa/YiJianFa.exe`。
- 包内包含 Chromium 和 FFmpeg；账号数据库、会话、OAuth 令牌、卖家工具私钥等禁止运行数据检查通过。
- Git 候选版本同时保存 Mac、Windows 及各自 SHA256，共四个规范资产；错误命名附件已移除。
- 旧 `v0.5.23-candidate-20260828` 已在确认本地可恢复副本和新候选下载校验通过后删除；Git 发布区现在只保留 `0.5.24`。

## 平台动作边界

- 本轮 Windows 构建和下载没有调用 TikTok、YouTube、Meta 或任何国内平台，没有上传、提交、定时或公开发布。
- TikTok 当前测试账号页面没有 Scheduler；这次打包不改变账号权限，也不证明原生定时发布已经可用。

## 尚未证明

- 已证明 Windows Runner 上的源码、打包程序和内置浏览器可启动；尚未在大帅的真实 Windows 电脑上完成双击启动和账号操作回读。
