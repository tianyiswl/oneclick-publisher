# 一键发 0.5.26 macOS 客户端验证记录

日期：2026-08-30（Asia/Shanghai）

## 本轮交付

- 源码：`integration/oneclick-0.5.25@4ece9e2` 加当前未提交工作树；不能冒充 Git 提交产物。
- 版本：`0.5.26`，包含 Facebook Page 任务 `91` 后续发现的列表延迟、临时空状态、相对时间、分钟精度、claim 二次校验和只读核对租约修复。
- 任务 `91` 仍是 `facebook_publish_outcome_unknown / ambiguous`，`blocksReplay=1`；本包不得导致该任务重放。

## 自动验证

- 版本与双端构建规则：`22/22` 通过。
- 版本升级后完整离线回归：`3068/3068` 通过，用时 `189.260s`。
- 源码和从 ZIP 解压后的应用均返回 `NATIVE_DESKTOP_UI_OK`。
- 从 ZIP 解压后的应用内置浏览器返回 `NATIVE_DESKTOP_BROWSER_OK`。
- `git diff --check`、arm64 架构、macOS 深度签名、ZIP 完整性、SHA256 sidecar 和客户包敏感运行数据门禁通过。

## Mac 包

- 应用：`release/macos-20260830-v0.5.26/一键发.app`。
- ZIP：`release/一键发_0.5.26_macOS_arm64_20260830.zip`，大小 `381929585` 字节。
- SHA256：`3f9fa0f012f23b62f02747cfb7a7498e6b9982cfb5f9970f034f3d9c7ebf1177`。
- `CFBundleShortVersionString=0.5.26`，`CFBundleVersion=0.5.26`，主程序架构为 `arm64`。
- 本地交付：`/Users/andy/文件/一键发/一键发_0.5.26_macOS_arm64_20260830.zip` 及同名 `.sha256` 文件，复制后哈希回读一致。
- 已替换安装到 `/Users/andy/文件/一键发/一键发.app` 并实际启动；原 `0.5.25` App 备份为 `/Users/andy/文件/一键发/一键发.app.backup-0.5.25-20260830-232340`。
- 关键运行数据库与发布配置备份位于 `/Users/andy/Library/Application Support/一键发开发版/backups/20260830-232340-0`；备份数据库和现用数据库快检均返回 `ok`。
- 安装路径回读版本 `0.5.26 / 0.5.26`、架构 `arm64`，深度签名通过，并返回 `NATIVE_DESKTOP_UI_OK`、`NATIVE_DESKTOP_BROWSER_OK`；实际运行进程来自该安装路径。
- 该包使用本机临时签名，不是 Apple 公证发行包，也没有生成升级系统签名 manifest。

## 平台动作边界

- 本轮测试、打包、解压验证、交付复制、安装和启动没有打开 Facebook、上传视频、点击最终动作或创建新发布任务。安装后活动发布任务、有效预检授权和有效直发授权均为 `0`。
- 任务 `89`、`91` 均不得自动重放；下一次真实验证必须使用全新内容、全新预检和新的当次授权。
