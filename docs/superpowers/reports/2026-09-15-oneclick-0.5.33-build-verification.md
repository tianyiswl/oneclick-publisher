# 一键发 0.5.33 内部测试包

日期：2026-09-15（Asia/Shanghai）

## 范围

用户授权打包与推送 Git。包含视频号批量内容准备、本地批次记录与取消；正式批量发布仍由 `FORMAL_AVAILABLE=False` 阻断。没有真实上传、预检、发布或安装替换。未纳入其他未完成的自动生产文档和 outputs。

范围检查返回 REVIEW_REQUIRED：本次为集成线审查，共享入口和独立批次模块经逐项确认后纳入，并未宣称符合国内定位专属范围。

## 当次验证

- 完整回归：3237 项通过，202.594 秒。
- 版本与 Mac/Windows 构建规则：22 项通过。
- 源码离屏界面：NATIVE_DESKTOP_UI_OK。
- 暂存差异检查通过。
- Mac 构建：MACOS_BUILD_OK；包内界面、内置 Chromium、临时签名及禁止运行数据门禁通过。
- Mac 包内版本：0.5.33；压缩包完整性通过；归档中已核对批次页面、历史、服务、队列和合同模块。
- Mac 文件：一键发_0.5.33_macOS_arm64_20260915.zip。
- Mac SHA-256：0c50492681c10531332d8df35051e880497d1209a2500102d5662c7b02f46e37。

Windows 构建结果待 CI 回读，未完成 Windows 真机验证。此版仅供内部测试，不代表视频号正式批量发布可用。
