# 本机受控发布接口与硅基探索 008 预检验收

日期：2026-08-24（Asia/Shanghai）

## 本轮完成内容

- 新增本机 CLI 受控发布入口，输入为内容包 manifest、明确平台账号、执行模式和各平台时间。
- 默认只能预检；正式发布必须绑定一条全部成功的预检任务，并使用十分钟内有效、只能消费一次的本地授权。
- CLI 与桌面页面复用同一发布服务和任务数据库；任务状态输出为稳定 JSON，不接收 Cookie、密码或验证码参数。
- 正式任务若触发抖音短信或扫码验证，CLI 只拉起现有本机原生验证窗口；验证信息仍只在进程内存中处理。
- 修复国内任务预检完成后误显示“海外平台”的按钮文案。
- 修复多平台预览 Markdown 被导入为通用正文的问题；旧 AI 声明结构显式迁移或给出清楚错误。
- 修复 macOS 抖音详情编辑器清空旧正文时使用错误快捷键的问题。
- 按 2026-08-24 小红书真实创作页更新视频封面入口和 AI 内容声明识别。

## 自动验证

- 受影响模块最终复跑：144 项通过，0 项失败。
- 最终完整命令：`.venv/bin/python -m unittest discover -p 'test_*.py'`
- 完整结果：1862 项通过，0 项失败，用时 64.796 秒。
- 离屏客户端：`NATIVE_DESKTOP_UI_OK`。
- `git diff --check`：通过。
- 改动 Python 文件编译检查：通过。
- CLI 状态查询：stdout 只有一行可解析 JSON；运行日志已改走 stderr。

## 008 内容包真实预检

使用现有客户端本地登录态，只执行预检，没有点击最终发布按钮。

1. 联合预检任务 `T08241748-6E7B`（taskId=8）
   - 抖音账号 4：预检成功，立即发布配置。
   - 小红书账号 6：首次因平台把 AI 声明改为“笔记含AI合成内容”而失败。
   - 这条任务最终状态是 `partial_failed`，不能用于正式发布授权。
2. 修复后小红书单项预检 `T08241755-D1D3`（taskId=9）
   - 小红书账号 6：预检成功。
   - 标题、正文、7 个官方话题、AI 声明、视频封面入口和 `2026-08-24 20:30` 发布时间均完成页面回读。
   - 任务状态仍是 `preflight`；没有保存草稿、没有提交定时、没有公开发布。
3. 内容字段核对
   - 抖音和小红书分别使用各自的标题、正文和话题。
   - 通用正文为空，不包含 `## 抖音`、`## 小红书` 等预览章节。
   - 抖音正文保留独立提及字段，不再由预览 Markdown 重复注入。

## 尚未执行

- 没有为 008 创建正式发布授权。
- 没有执行抖音或小红书正式提交。
- 没有取得平台作品 ID、定时成功回执或公开链接。
- 若要正式发布，必须先重新跑一次两平台都成功的联合预检；大帅确认后才能生成一次性授权并启动正式任务。
