# 一键发 0.5.23 macOS 客户端验证记录

日期：2026-08-28（Asia/Shanghai）

## 本轮交付

- YouTube 使用 Google 官方桌面 OAuth 与 Data API，不保存 Google 密码、Cookie 或刷新令牌到项目文件。
- 账号管理页可回读频道名、频道 ID 和官方头像；“打开后台”按保存的公开频道 ID 进入同一 YouTube Studio。
- 受控发布通道默认私密，正式上传必须明确选择儿童内容声明；支持封面、不通知订阅者参数和精确视频 ID 回读。
- 修复 YouTube 分块上传完成时返回 HTTP 200 被误判为结果不明的问题；HTTP 200 与 201 都必须同时取得精确视频 ID 才能进入回读。

## 自动验证

- YouTube API、发布服务、版本与双端构建规则：`73/73` 通过。
- 账号后台入口与官方头像：`23/23` 通过。
- 防休眠条件下完整离线回归：`2259/2259` 通过，用时 `82.564s`。
- 源码、构建后应用和安装后应用的离屏界面均返回 `NATIVE_DESKTOP_UI_OK`。
- `git diff --check` 与 macOS 深度签名验证通过。

## Mac 包与安装

- 应用：`release/macos-20260827-v0.5.23/一键发.app`。
- ZIP：`release/一键发_0.5.23_macOS_arm64_20260827.zip`，大小 `381568577` 字节。
- SHA256：`a97c21a8cdd52d64924561d537d73159a6d75f74779d49dd9e5c88848d0ae68b`。
- `CFBundleShortVersionString=0.5.23`，临时签名、ZIP 完整性和包内界面启动检查通过。
- 本机已安装到 `/Users/andy/文件/一键发/一键发.app`，并实际启动账号页；原 `0.5.22` 保存为 `/Users/andy/文件/一键发/一键发_0.5.22_backup.app`，可回滚。
- 安装后仍回读到同一 YouTube 频道 `Mobai`，OAuth 权限版本为 `2`，头像是可解码的 `800x800` PNG。

## 真实平台边界

- 零写入预检任务 `55 / T08272337-777A` 成功，回执明确没有平台变更和视频 ID。
- 大帅单独授权后，正式任务 `56 / T08272339-B567` 只执行一次上传。旧代码将有效 HTTP 200 误判为失败，但 YouTube 官方 API 与 Studio 均回读到同一精确视频，状态为私密、非儿童内容且处理完成。
- 没有第二次上传；封面只在精确视频 ID、频道、标题和私密状态全部符合后补齐。任务 `56` 的原始失败记录保留，不改写历史。
- 本记录只证明“单个私密视频”已真实验收；公开、不公开和定时公开仍只有本地测试，未进行真实平台验收。
