# 一键发 0.5.3 macOS 候选包验证记录

日期：2026-08-24（Asia/Shanghai）

## 变更范围

- 源码提交：`133d7bf`
- 分支：`integration/oneclick-0.5.0`
- 版本：`0.5.3`
- 修复抖音预检与正式发布的话题候选/回读差异。
- 修复小红书视频封面控件、异步话题候选和最终按钮点击区域。
- 为受控发布补充稳定错误码、进程心跳和失联任务终态收口。

## 自动验证

- 完整测试：`1881/1881` 通过，命令 `.venv/bin/python -m unittest discover -p 'test_*.py'`。
- 离屏界面：`NATIVE_DESKTOP_UI_OK`。
- macOS/Windows 构建安全测试：`19/19` 通过。
- `git diff --check` 通过。

## 真实平台证据

- 抖音正式任务 `T08242113-E510` 已从作品管理页回读 `2026-08-25 09:00`，记为定时成功。
- 小红书修复后预检 `T08242131-D5AD` 成功。
- 大帅重新授权的小红书正式任务 `T08242137-45E5` 已返回定时发布成功，时间为 `2026-08-25 09:00`。
- 上述是平台已受理定时任务的证据；在到期后读到作品列表或公开页面前，不写成已公开上线。

## macOS 产物

- 应用：`release/macos-20260824-v0.5.3/一键发.app`
- 压缩包：`release/一键发_0.5.3_macOS_arm64_20260824.zip`
- 大小：约 `353 MB`
- SHA256：`a794b4e587d7cbd4f00801307a6574dba1750cd0c1b4d979647397ced57bf6cf`
- `CFBundleShortVersionString` 和 `CFBundleVersion` 均为 `0.5.3`。
- 临时签名、离屏界面、内置浏览器、应用自检、ZIP 完整性和敏感运行数据检查全部通过。

## 边界

- 这是 Mac 候选包，尚未替换 `/Users/andy/文件/一键发/一键发.app` 中的 `0.5.2` 已安装客户端。
- Windows `0.5.3` 本轮未构建，最新 Windows 候选仍为 `0.5.1`。
