# 一键发 0.5.7 macOS 候选包验证记录

日期：2026-08-25（Asia/Shanghai）

## 修复结果

- 根因：Mac 打包后 `Frameworks/ui` 是指向 `Resources/ui` 的包内软链接，旧代码在解析真实路径后把合法探针误判为“路径不在资源目录内”。
- 修复：冻结版从应用包 `Contents/Resources` 读取探针；源码模式和测试显式传入目录仍执行原有越界、文件、大小及 MP4 校验。
- 诊断：采集器启动失败会记录脱敏后的固定错误和安全根因，不向公开结果暴露账号文件、Cookie 或页面原文。

## 自动测试

- `test_douyin_commerce_probe.py` 与 `test_douyin_commerce_collectors.py`：`109 passed, 49 subtests passed`。
- `test_macos_build.py`：`5 passed`。
- 完整 `unittest` 回归：`1917 tests`，全部通过，用时 `77.709s`。

## 打包包真实验证

- 应用：`release/macos-20260825-v0.5.7/一键发.app`。
- ZIP：`release/一键发_0.5.7_macOS_arm64_20260825.zip`。
- SHA256：`9ebaf243d3916423ddfb57a3302ce6a0c8f78cba57d1b8e876ebb5c254140c09`。
- `CFBundleShortVersionString=0.5.7`，`codesign --verify --deep --strict` 通过。
- 包内探针存在，大小 3338 字节。
- 直接运行打包应用，使用本机已保存账号和内容点击“上传视频并继续”，已进入抖音带货“平台设置”，页面显示收藏音乐和逐条地点设置。
- 验证到平台设置即停止并正常关闭客户端；没有检查提交、定时或正式发布。

## 尚未执行

- 没有覆盖安装当前 `/Users/andy/文件/一键发/一键发.app`。
- 没有生成 Windows 0.5.7 包。
