# 一键发 0.5.9 macOS 候选包验证记录

日期：2026-08-25（Asia/Shanghai）

## 本轮交付

- 增加本机 stdio MCP 主通道，五个内容项目可通过结构化内容包、项目标识和各平台时间创建受控任务，不再依赖逐个点击桌面控件。
- 项目标识在一键发本机映射到明确平台和账号 ID；映射文件不保存 Cookie、密码、验证码或会话路径。
- 预检与正式发布为两个独立工具。正式发布必须绑定全部成功的预检，并消费短期一次性授权。
- MCP 复用现有受控发布服务和 CLI 子进程；平台验证仍使用一键发原生窗口，敏感内容不经过 MCP。
- 发布中心已把硅基进化公众号入口改称“兼容通道”，并明确它只用于 V1.2 冻结包保存草稿，不是五个项目的通用主通道。

## 自动测试

- 新增能力开发阶段定向测试：`52/52` 通过；收尾时重新运行网关、子进程、MCP、界面和两端打包规则测试：`31/31` 通过。
- 最终完整 `unittest` 回归：`1929/1929` 通过，用时 `80.905s`。
- 子进程和两端打包规则在 `ResourceWarning` 视为错误的条件下复跑：`21/21` 通过。
- `git diff --check`、集成支线范围检查、`pip check`、`compileall` 和离屏客户端均通过。

## 打包与包内接口验证

- 应用：`release/macos-20260825-v0.5.9/一键发.app`。
- ZIP：`release/一键发_0.5.9_macOS_arm64_20260825.zip`。
- SHA256：`e690ed6ddb6a6edb0db3871c61400053dbb89f9300452c0b8222b140dc8dce84`。
- `CFBundleShortVersionString=0.5.9`，`CFBundleVersion=0.5.9`。
- `codesign --verify --deep --strict`、ZIP 完整性、固定 Chromium 和包内界面自检均通过。
- 使用 MCP 官方客户端直接启动包内 `一键发 --mcp-server`，成功识别服务名 `yijianfa-local`、列出 7 个受控工具，并完成账号列表只读调用。

## 状态边界

- 本轮没有安装或替换 `/Users/andy/文件/一键发/一键发.app`。
- 本轮没有为五个内容项目写入正式项目账号映射。
- 本轮没有执行真实平台预检、正式提交、定时或公开发布。
- 本记录只证明 0.5.9 Mac 候选包与本机 MCP 接口可运行，不代表任何内容已经发布。
