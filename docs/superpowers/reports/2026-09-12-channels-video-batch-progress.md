# 视频号批量视频实施进展

本报告记录部分实现，不是正式发布验收。

## 任务记录接线验收

已在任务记录页增加“视频号本地批次”独立入口，直接读现有队列，显示最近100批和逐条状态/排期/错误/回执，仅允许取消待执行项。读取失败保留并明确标注旧详情归属；批次级清理错误可见。两个独立审查问题修复后复审通过。完整离线回归3237项通过（194.668秒，`/tmp/oneclick-history-final.log`），客户端离屏启动正常、差异检查通过。源码可测试此本地功能；真实批量发布仍未开放，未打包、未推送、未发表。

## 本轮离线执行接线

最终核验：完整离线发现 3229 项通过（193.257 秒，`/tmp/oneclick-video-batch-final-regression.log`）；该轮之后最后补齐的逐条恢复原因由新鲜定向 45/45（2.025 秒）覆盖，不把前面的全量数量当作最后新增用例的全量。离屏启动 `NATIVE_DESKTOP_UI_OK`，差异检查通过；Qt 仍输出既有 Sans Serif 字体替换提示，不是失败。独立复审最初发现 4 处恢复/异常缺陷，修复并复审后全部关闭。未提交、未推送、未打包、未访问平台。

新增共享本地服务 `batch-prepare / batch-status / batch-cancel` 与界面本地批次按钮；生成不可变批次后可通过独立进程读取和取消未执行条目。新增可注入模拟适配器的串行执行器，包含运行期心跳、提交意图持久化、提交后结果不明暂停与去重保护。草稿非法类型及后台检查期间关闭窗口的保护已补充。

真实平台适配器、通用任务记录列表与正式续跑仍未接通；`batch-authorize / batch-start` 固定拒绝并返回 `video_batch_formal_unavailable`。本轮交付范围是离线编排，不能用于真实批量发布。以下首轮状态保留作为历史记录，不代表当前接线完全未开始。

已接通发布中心准备窗口与 CLI batch-check。可准备一个账号的 1–20 个视频，设置通用内容及逐字段覆盖、封面和排期，保存/恢复本地草稿。检查在后台线程计算文件哈希；编辑期间变化的结果不回填。

新增批次队列原型提供一次性授权消费、执行租约、提交意图、崩溃恢复和结果状态，并通过模拟测试验证；尚未接入平台运行、已有任务记录和 CLI 正式执行。单次提交函数要求持久化先于点击、点击后只读回执，无回执为结果不明；旧视频号上传器仍未调用它。

验证：完整离线发现 3207 项通过，197.238 秒。该轮之后追加的提交边界、队列终态和 CLI 用例由最后定向测试覆盖，不把旧全量数量称为最终全量。源码离屏界面返回 NATIVE_DESKTOP_UI_OK。真实 CLI 进程在隔离临时目录与虚构账号中成功返回 local_check_passed、platformWriteOccurred=false、formalAvailable=false；素材为合同测试文件，不是可播放视频或平台上传证据。

Archify 目标架构图已交付，9/9 验证通过、0 errors/0 warnings；四种桌面尺寸自动无溢出。视觉截图仍未人工审核，不声明视觉验收通过。图表示目标边界，不代表模块全接通。

待完成：当前视频号账号登录回读和页面取证；平台实体、封面及唯一回执适配；执行器、暂停/恢复、已有任务记录与正式 UI/CLI；最终代码全量回归；具体内容授权后的真实小批次验收。本轮没有浏览器登录、平台预检、上传或正式发表。
