# 一键发 0.5.28 Facebook 基线修复与 Mac 构建验证

## 范围

- 集成分支：`integration/oneclick-0.5.28`。
- Facebook 修复提交：`753d558`；版本候选提交：`f9f76fd`。
- 修复内容：Meta 无时区时间回读、延迟出现的“创建至今”筛选、React 虚拟表格瞬时缺行及滚动节点替换。
- 排除：Windows 构建、任务 `97` 重放，以及未经重新确认的 Facebook 最终发布。

## 原故障与安全边界

- 任务 `96 / T08310950-2020` 预检成功。
- 大帅对任务 `96` 的一次性公开发布授权被任务 `97 / T08310952-2111` 消费。
- 任务 `97` 在最终动作前因 `facebook_page_baseline_read_failed` 安全失败；`finalActionTriggered=false`、`platformWriteOccurred=false`，没有上传、最终点击、Reel ID、URL 或发布时间。
- 原因是 Meta 内容列表只提供无时区时间，且虚拟表格在读取和滚动期间会替换行节点；旧实现把两者都当作稳定 DOM 处理。
- 修复后使用正式 Page 登录会话连续执行两次只读基线读取，两次均成功，当前列表为 `0` 条 Reel；没有打开编辑器、上传视频或点击发布。

## 源码验证

- Facebook 定向测试：`115/115` 通过。
- 完整离线回归：`3078/3078` 通过，用时 `254.811s`。
- 版本与 Mac/Windows 构建规则：`33/33` 通过。
- 源码离屏界面：`NATIVE_DESKTOP_UI_OK`。
- 源码内置浏览器：`NATIVE_DESKTOP_BROWSER_OK`。
- 相关文件编译与 `git diff --check`：通过。
- 范围检查把海外实现识别为 `owned`；版本文件为集成线允许修改的 `shared`，两份 Facebook 测试因检查器文件名规则被列为 `outside`，已按 `QUALITY_GATES.md` 的海外对应测试范围人工复核。

## macOS arm64 候选与安装

- 构建应用：`release/macos-20260831-v0.5.28/一键发.app`。
- 候选 ZIP：`release/一键发_0.5.28_macOS_arm64_20260831.zip`。
- 本地交付：`/Users/andy/文件/一键发/一键发_0.5.28_macOS_arm64_20260831.zip`。
- SHA256：`d0b5ad26c7053291dee55efdcdcec3434013a51cead9956633f2472d5afc16b2`。
- 包内与安装后 `CFBundleShortVersionString`、`CFBundleVersion` 均为 `0.5.28`，主程序为 `arm64`。
- 深度签名、包内界面、内置 Playwright Chromium、ZIP 完整性及构建脚本运行数据门禁通过。
- 安装前数据库备份：`/Users/andy/Library/Application Support/一键发开发版/backups/20260831-104517-0`。
- 旧 App 备份：`/Users/andy/文件/一键发/一键发.app.backup-0.5.27-20260831-104517`。
- 安装后数据库 `quick_check=ok`，14 个账号保持不变，活动任务和未消费授权均为 0。

## 0.5.28 真实预检

- 内容包：`runtime/facebook-page-formal-acceptance-20260831-v6/manifest.json`。
- 标题：`Facebook Page 0.5.28 正式验收 P6`。
- 唯一编号：`FB0528-P6-0831-1048`。
- 目标：Facebook Page `1200693729803807`，本机账号 ID `14`，显示主体“墨白”，公开范围，无排期。
- 任务 `98 / T08311049-DC2D` 状态为 `success / platform_form_verified`。
- 视频、正文、公开范围和可用最终按钮均已回读；预检发生临时媒体上传，因此 `platformWriteOccurred=true`，但 `finalActionTriggered=false`。
- 没有创建正式授权，没有点击最终发布按钮，没有 Reel ID、URL 或发布时间。

## 当前结论

`0.5.28` 已完成源码、本地真实只读、Mac 构建安装和新内容预检验证。它仍未证明 Facebook Page 正式公开发布成功；下一步只能在大帅对任务 `98` 再次明确确认后创建新的短期一次性授权，并只执行一次正式动作。
