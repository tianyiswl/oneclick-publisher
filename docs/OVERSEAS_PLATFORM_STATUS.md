# 海外平台接入状态

## 统一执行原则

海外平台统一使用一键发自己的本地浏览器会话，不要求用户申请开发者应用，
也不提供开发者配置或接口授权入口。登录、会话保存、素材填写与结果记录都在
一键发客户端内完成，不需要启动蚁小二客户端。

| 平台 | 账号登录与检测 | 视频预检 | 正式动作 | 当前边界 |
| --- | --- | --- | --- | --- |
| TikTok | 可见登录、自动保存、静默检测 | 上传并填写文案，停在 Post 前 | 未开放 | 不创建草稿，不点击 Post |
| YouTube | 可见登录、自动保存、静默检测 | 上传视频/封面并填写字段，停在 Done 前 | 未开放 | 不点击 Done/Save；定时尚未完成回读 |
| Instagram Reels | 与 Facebook 共用 Meta 会话 | 选择 Instagram 目标并填写 Reels | 可见浏览器二次确认后执行 | 验证码、双重验证、风控或未知提示会停止 |
| Facebook Reels | 与 Instagram 共用 Meta 会话 | 选择 Facebook 目标并填写 Reels | 可见浏览器二次确认后执行 | 只有明确平台回执才记为成功 |

## 客户端入口

- “绑定账号”在一键发独立浏览器会话中打开平台官网，用户完成登录后自动保存。
- “检测登录”默认静默，只检查已保存会话；异常时提示用户手动重新登录，不自动弹出登录页。
- Instagram 与 Facebook 共用一次 Meta 登录，但在账号列表和发布目标中分别显示。
- 账号列表只保存平台、主体、显示名、本地会话引用与检测状态。

## 当前发布门槛

1. 海外恢复流程当前只验收视频，图文和纯文字不会创建伪任务。
2. TikTok 与 YouTube 只允许预发布检查，正式按钮保持锁定。
3. Instagram/Facebook 正式发布必须保持浏览器可见，并完成两项一次性确认。
4. 无法稳定回读的选项会在上传前阻断，不会静默忽略。例如 YouTube 关闭订阅者通知、Instagram 仅 Reels 不分享到动态、Meta AI 声明。
5. 只有平台返回明确发布或定时成功证据，对应执行项才会记为成功。

## 尚未完成的真实验收

当前版本完成了源码接入、界面门控和离线回归，尚未使用真实海外账号与测试视频
执行平台验收。下一步需要在一键发中逐个平台绑定专用测试账号，再验证身份、字段、
素材、最终按钮前状态以及平台回执。

## 安全边界

- 二维码、Cookie、storage state 和账号凭据不写入代码仓库、安装包、发布包或长期日志。
- 登录失效、验证码、二次验证、风控和未知合规提示都是安全停止条件。
- 平台动作失败或回执不明确时不会自动盲目重试，避免重复发布。
